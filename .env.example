# ローカル開発 (SQLite)
DATABASE_URL="file:./dev.db"

# 本番 (Supabase PostgreSQL) の例:
# DATABASE_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT].supabase.co:5432/postgres"

# セッション署名用シークレット (本番では必ずランダムな値を設定)
# 生成例: openssl rand -base64 32
AUTH_SECRET=""
