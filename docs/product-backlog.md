# TacTap プロダクトバックログ (PM管理)

運営方針: 超リアルなペルソナによる外部評価サイクルを回し、
大玉から徹底的に改善する。機能の改廃も躊躇しない。

## 運営方針の改訂 (サイクル15〜: 根本改善ループへ)
サイクル11〜14で必需3→6〜7まで改善したが、独立評価が3回連続で「9点の壁は
コミュニティのデータ密度(コールドスタート)であり、設計は誠実で正しく、偽データは禁じ手」
と収束。バグ潰し型のループは頭打ちと判断し、**プロダクトの根本価値を問い直すループ**に
切り替える。問い直すべき論点 (ユーザー指示):
- **A/B強制選択そのものが価値なのか**: 作業感があり、疎なデータでは結論が出ない。
  別のコア体験(相対マップの閲覧・定性レビューの消費)の方が必需度が高い可能性。
- **マイギア中心が正しいのか**: 個人の使用履歴を軸にするのは正しいが、それが
  コア価値の置き場として最適か。
- **データから導く「全用具の相対マップ」**: 蓄積したA/B比較から、推移性を使って
  疎なデータでも全用具の相対位置(軸別)を導きダッシュボード化する。同じデータから
  より多くの価値を引き出し、コールドスタートを部分的に克服できる仮説。
- **定性レビューの深堀り**: 体感メモ/feel statements を、用具選びの意思決定に
  効く深さまで掘れる構造にする。
→ 評価プロンプトも「バグ探し」から「根本的な価値提案の妥当性と、最も必需度を上げる
  構造転換の提案」に訂正する (下記サイクル15のプロンプト参照)。

## 評価サイクル記録

| # | ペルソナ | 実施 | スコア | 主要発見 |
|---|---|---|---|---|
| 1 | 高橋健太 (34・上級・ドライブ・ディグニクス使い) | 済 | 3.5/10 | 厚さ上書きバグ / n=1ランキング有害 / 正規化値が信頼毀損 / 面跨ぎ比較 / 回答が資産にならない |
| 2 | 高橋健太 (同・アンカー再評価) | 済 | 必需5 / 手軽6 / 手間5 | 前回5指摘中4が解消を確認。新規: switchの無反応感 / n=1断言が用具ページ等に残存 / 重複登録ガード |
| 3 | 高橋健太 (同・アンカー再評価) | 済 | 必需5 / 手軽6 / 手間5 (横ばい) | 重複ガード・出し尽くしバナー・集計中表示は確認。新規: switchがマイギア未参照 / 勝率n=5断言 / 9点への決定的不足=「データが育つ過程が見えない」 |
| 4 | 高橋健太 (同・アンカー再評価) | 済 | 必需6 / 手軽6 / 手間6 | サイクル3対応(switch基準自動設定・公開進捗の統一表示)を確認、信頼回復の手応え。新規(致命): 上級と答えたのに/meが「中級・決まっていない」表示=プロフィール不整合 / 診断の「全プレイヤーの100%が該当」がn=1で嘘くさい / BH面1本のロゼナが永遠に出題されず理由の説明もない |

| 5 | 高橋健太 (同・アンカー再評価) | 済 | 必需7 / 手軽7 / 手間6 | サイクル4対応(プロフィール一貫性・診断の正直さ・BHナッジ)を確認。新規: /switchの「みんなの好み」がn=1で100%/0%断言(リストと不整合) / compareインサイトもn=1で「100%と多数派」 / 本丸=switchが現用FH固定でBH(ロゼナ)の乗り換え検討が不可能 / /meの回答履歴に自分の判定が見えない |

| 6 | 高橋健太 (同・アンカー再評価) | 済 | 必需8 / 手軽8 / 手間7 | 基準切り替え・n断言一掃・判定チップを確認、「自分のデータが信用できる」状態に。新規: 用具ページが行き止まり(気になる→検討に直結しない) / ギア登録が検索入力必須(M3) / ロゼナ基準だとワンタップ候補ゼロ(コールドスタート) |

| 7 | 高橋健太 (同・アンカー再評価) | 済 | 必需8 / 手軽9 / 手間7 | ワンタップ登録・用具ページ直結を確認、登録と検討の摩擦はほぼ解消。新規(重大): 回答済みの質問が再出題される(既出除外が直近20件のみ) / 候補を並べた後の「で、どれ？」に答えるまとめがない / 体感メモが/gearの奥でマイページから見えない |
| 8 | 高橋健太 (同・最終評価) | 済 | ~~必需9 / 手軽9 / 手間9~~ **(無効・自己評価)** | E2E検証(n=3公開遷移・出し尽くし宣言・再出題ゼロ)は事実だが、採点は実装者自身による同一コンテキストの自己評価であり中立性なし。サイクル9の独立評価により撤回 |
| 9 | 高橋健太 (**独立評価**: 会話履歴・修正内容・合格基準を知らない別エージェントがHTTP経由のみで実走) | 済 | **必需3 / 手軽6 / 手間4** | 新規発見(自己評価が見落とした実欠陥): ①同一ユーザーの連投でnと%を水増し可能(致命) ②ギア/回答がセッション紐付きで別デバイスログインで消失(「引き継がれます」の約束違反) ③パスワード不在(メールだけで他人のデータにアクセス可) ④厚さ別集計なし ⑤LP「両方使った人だけ」が実態(イメージ回答も収集)と乖離 |

| 10 | 高橋健太 (**独立評価2回目**: 同条件で切離し) | 済 | 必需3 / 手軽6 / 手間4 (横ばい) | サイクル9修正を実走確認(連投upsert・デバイス引き継ぎ・PW拒否はすべて堅いと評価)。新規(致命): 同一ユーザー連投は塞いだが、**匿名セッションの量産**でn・%・看板・ランキングを水増し可能(/api/onboardingが無認証・無制限で新セッション発行)。救い=経験フラグはサーバ計算で詐称不能、/compare既定の「両方使った人のみ」は無汚染。本命ロゼナ(バック面)は質問生成されず行き止まりのまま |

| 11 | 高橋健太 (**独立評価3回目**: 同条件で切離し。サイクル11実装後) | 済 | 必需3 / 手軽6 / 手間5 | サイクル11修正(バック面巡回出題)で実体験モード・両方使用フィルタは確認。新規(最優先・致命): 自分でロゼナvsラクザ7を全軸答えても各軸n<3で「集計中」のまま隠れ、**自分の判定すら見られない**。+ /switch初期基準が現用FH固定でBHに手動切替要 / 登録した候補が/switch候補に未反映 / answerCount表示の出所不審 |

| 12 | 高橋健太 (**独立評価4回目**: 同条件で切離し。サイクル12実装後) | 済 | **必需7 / 手軽7 / 手間5** (必需3→7の大幅改善) | サイクル12「あなたの判定」を高評価(回答通り軸別に正確・本人記録と他者集計を明確分離)。必需が3→7に。新規(最優先): FH+BH現用2本でも実体験対決が出ず全部イメージ回答なのに「ギア内の比較は出し尽くしました」と**誤表示**(理由説明なし) / /switchが回答済み候補を自動表示せず空状態 / 回答API tallyが軸無視で常にoverallを返すバグ |

| 13 | 高橋健太 (**独立評価5回目**: クリーンDB・コールドスタート。サイクル13実装後) | 済 | **必需6 / 手軽7 / 手間7** (手間5→7) | サイクル13修正(誤表示是正・候補自動表示・軸別tally)・自分の判定の可視化・誠実なn表示・スペック不信への配慮を高評価。最優先①=自分で答えてもコミュニティ結論が0件(=**データ密度の問題・設計は誠実で正しい・実装では直せない**)。②最初がイメージ質問(実装で直せる→サイクル14対応) ③/me空(誤診=未ログインのリダイレクト) |

### サイクル14 対応 (独立評価サイクル13の指摘。実装で直せる分のみ対応)
独立評価5回目: **必需6 / 手軽7 / 手間7**(手間5→7)。評価者の結論=「9点の壁の本質は
①コミュニティのデータ密度の問題(設計は誠実で正しく、偽データ投入は禁じ手)。②③は実装で直せる」。
- **② 最初の質問が現用と無関係なイメージ質問**: ギア未登録で /play に来ると、いきなり
  ランダムな2本のイメージ質問が出て、両方使った人の比較を期待した第一印象を損ねていた。
  - 修正(`play/page.tsx`): gearCount===0 のときはランダム質問ではなく
    **ギア登録ゲート**を表示。「まず使ったラバーを登録(同じ面で2本以上)」を促し、
    `?explore=1` で明示的にイメージ回答も試せる導線を残す。
  - 検証(実走): ギア0本 /play でゲート表示・イメージ質問は非表示、
    /play?explore=1 でイメージ質問に進めることを確認。
- **③ /me が空(評価者の誤診)**: /me はサーバコンポーネントで本来SSRされる。評価者が
  空を見たのは匿名セッションで未ログイン→/loginリダイレクトのため(設計通り、匿名は/gear)。
  実バグではないため変更なし。
- **① コミュニティ結論が0件**: コールドスタート=実データ密度の問題。評価者自身が
  「設計は誠実で正しい」と明言。コードでは解決不可(偽データは禁じ手)。GTM/初期データ
  獲得の経営課題であり、実装サイクルの守備範囲外。次セクション「9点の壁」に整理。

### 9点の壁の構造的分析 (サイクル14時点)
- サイクル11〜14で必需3→6〜7・手軽6→7・手間4→7まで改善。器(乗り換え動線・自分の判定の
  可視化・誠実なn表示・スペック不信への配慮)は独立評価で一貫して信頼を獲得。
- 残る最大の壁=「両方使った他人の結論」が0件。これは実装ではなく**実ユーザーの蓄積**が
  必要で、偽データ投入は企画書のブランド指針・本バックログの運用ルールに反する禁じ手。
- よって必需9達成にはコード改善ではなく、初期ユーザー獲得/データ獲得施策(GTM)が要る。
  実装で残る価値はUXのポリッシュ(②のような第一印象改善)に限られる。

### サイクル15 (根本転換): 相対マップ — 3票ゲートを撤廃し疎データから結論を出す
独立評価6回目(**根本価値の問い直し**。バグ探しでなく中核設計の妥当性を問う)。
評価者の必需スコアは**1.5/10**(単独ユーザー・疎データでは意思決定材料が一切出力されない)。
- 評価者の診断: **病巣はA/B比較という手法でなく、結論を全部「ペアに3票溜まったら」に
  先送りする集計モデル**。switch空・自分の票が返らない・全出口が3票ゲートで、
  単独ユーザーは構造上永遠に結論に到達できない(=コールドスタートで死ぬ設計)。
- 評価者が選んだ最優先の構造転換=**仮説(A) 推移律ベースの全用具・軸別相対マップ**。
  同じA/B入力をペア勝率でなく1枚の順序推定に合成すれば、A&gt;B・B&gt;C ⇒ A&gt;C で
  疎データでも全体地図が描け、1票が地図全体に寄与する。マイギアは回答ノルマの起点でなく
  地図上の原点(現在地マーカー)に。到達見立て: (A)実装で必需7、(C)定性深堀り併用で8.5。
- **実装**:
  - `lib/ranking.ts`: 正則化Bradley-Terry(MM法)。全比較を1つの強さ推定に合成。
    仮想アンカー(平均ラバー)と各項目をalpha回引き分けさせ、(1)グラフ分断でも単一
    スケール (2)全勝/全敗でも有限値 (3)支持薄は中央へ縮約、を同時に満たす。
    比較ごとに信頼度重み(実体験1.0/イメージ0.4)。`__tests__/ranking.test.ts`で
    推移律(A&gt;B,B&gt;C⇒A&gt;C)・矛盾比較の互角収束・信頼度効果を検証。
  - `lib/relative-map.ts`: DBの全比較→軸別(好み/速/スピン/硬/球持ち)の相対マップ。
    ラバー同士のみ。各ラバーに実比較本数と「両方使った人」本数を付与。
  - `app/map/page.tsx` + ナビ導線: 軸タブ・相対位置バー・マイギアを原点フラグ
    (使用中/マイギア)・支持本数(実◯/全◯件)・支持<3は「中央寄りの暫定位置」と明示。
    3票ゲートを撤廃し「推定位置+信頼度」で常に何かを返す。空軸は正直な空状態+導線。
  - 検証(実走): ロゼナ&gt;テナジー05・テナジー05&gt;ラクザ7 を投入→ /map?axis=speed で
    **ロゼナ&gt;テナジー05&gt;ラクザ7**(ロゼナとラクザ7は直接対決なし=推移律が実証)。
    複数セッションの比較が同一地図に合成され、マイギアが「使用中」でフラグされることを確認。
- 次の論点: (C)定性レビュー深堀り(候補3本に絞った後の最終確認層)、推移律推定の信頼性
  (循環・矛盾の扱い、信頼区間の可視化)、/switch・compareをマップ基盤へ統合。

### サイクル16 (再設計v2): コア価値=相対理解への作り直し / 独立評価 必需1.5→6.5
ユーザー再定義「コア価値=用具の特徴が相対的に分かる / A/Bは手段で主役でない /
入力は閲覧のために自然に求める」を受けた全面再設計。詳細は `redesign-v2-direction.md`。
- **企画立ち戻りエージェント** `.claude/agents/planning-anchor.md` 新設(判断をコア価値に
  立ち戻って検証)。**実リサーチ3本**(評価軸/マイギア管理/SNS)を出所つきで反映。
- **funnel反転**: ホームの主役を「用具マップを見る」に、A/Bを副導線へ。新規もまず見てから登録。
- **比較項目**: control を相対マップに露出、**弧線(arc)軸**を新規追加(schema/質問生成/マップ/
  詳細/履歴に貫通)。客観属性(原産国/表面/重量/価格)はフィルタ化を後続に。
- **マイギア再設計**: usageStartedAt(貼付日・現用登録時に自動設定)+ note、張替リマインド、
  「現在の構成/遍歴」分割、貼った日・メモのインライン編集。gear-reminder純関数+テスト。
- **用具詳細のハブ化**: 相対マップ上の軸別位置(順位+支持本数)・ギア比・レビュー動画(YouTube)・
  シェア。**SNSシェア**: 用具の動的OGカード(ImageResponse)+ Web Share→X。SNS投票は本体DBに
  混ぜない指針。
- **独立評価7回目(再設計後・クリーンDB)**: **必需1.5→6.5(+5.0)**。「コア価値は体験として成立、
  推移律も評価者が実機確認、致命傷(疎データで何も出ない/作業感)は構造的に解消」。
  9点への最優先=「/switch(ペルソナの本丸)が直接対決依存でコールドスタートでは手応えが出ない」。
- **最優先指摘に即対応**: `buildSwitchCandidates`(基準に対する全ラバーの軸別差分を推移律で算出)
  で /switch を相対マップ基盤に。直接対決なしでも軸別 ▲/▼/≈ つき候補が疎データで出る。実走検証済み。
- 次の論点(必需6.5→9): 軸別データ偏在の緩和(出題の最適配分)、定性レビュー深堀り、信頼区間の可視化、
  出題の「両方使った?」昇格、診断/ギア専用OGカード。

### サイクル16の評価反復 (独立評価7〜10回目。各指摘を実装で即対応)
再設計後、コア価値が成立したかを独立評価で連続検証し、出た実装可能な指摘を1つずつ潰した。
- **評価7 必需6.5**: 「コア価値成立・推移律を評価者が実機確認・致命傷(疎データで無)は構造的に解消」。
  最優先=/switchが直接対決依存でコールドスタート空 → **buildSwitchCandidates で相対マップ基盤化**。
- **評価8 必需5**: 定性レビュー追加を確認も、致命バグ発見=「両方ギア登録後の声が"イメージ"表示」。
  → answers API更新分岐で **hasActualExperience を毎回再判定**。加えて差分が方向のみで量が無い指摘
  → /switch に **相対位置差(0-100)** を追加。
- **評価9 必需6**: バグ修正と量を確認。最優先=「量に確信度が無く、疎データの飽和値(▼-100)が
  堅牢な推定と同じ見た目」 → /switch チップに **支持本数n併記・薄い根拠はグレー縮約・参考程度バッジ**。
- **評価10 必需6**: 確信度設計を「優秀」・定性レビュー「完全に通る」・推移律「効く」と確認。
  残る決定的要因は**構造的なデータ供給**: (a)nは人単位で単独ユーザーは閾値を越えられず量が出ない
  (=コールドスタートの正直な現実) (b)投稿口がoverall固定で軸別nが枯渇 → **体感フォームを軸別投票へ
  拡張**(好み+任意で各軸)。
- **到達点と結論**: 必需1.5→6.5→(バグ発見で5)→6。**方向・推移律・定性レビュー・経験フラグ・確信度表示は
  9点級**。残る6→9の壁は、サイクル14と同じ**実ユーザーのデータ密度(コールドスタート)**に収束。
  軸別投票UIで供給面の枯渇は塞いだが、量(数値差)の確信は複数ユーザーの蓄積を要する=GTM課題。
  偽データ投入は禁じ手。コードで残る価値は供給導線とUXのポリッシュに限られる。

### サイクル23 (独立評価17回目。新ペルソナ=用具沼の上級者。データ破綻疑いを検証=エンジン健全)
独立評価(斉藤亮・29・上級・ドライブ・シェーク裏裏・20枚以上試した用具沼・**必需4/手軽7/手間5**)。
大量入力(ギア11本・回答66問)で「入力が報われるか・解像度・近縁の差・履歴管理」を突いた。最大の指摘①「かたさ軸が
物理的に逆(D05/09CがT05より柔らかい判定)」を**決定論的に再現検証**した結果、**符号は正しくバグではない**:
- **検証(#1=評価方法のアーティファクト・コード変更なし)**: クリーンDBで「hardness: D05が硬い」を1件だけ回答→相対hardnessは
  **D05=100 / T05=0**(公称 D05 40°>T05 36° と一致)。winner→格納→スコアの符号は一貫。評価者が見た"逆転"は、評価者自身が
  hardnessをT05寄りに回答した結果を地図が忠実に反映したもの。**相対マップは公称でなく"使った人の体感"で動く設計**(コア価値/
  サイクル15)。評価者の改善案「公称硬度で符号を補正」は、公称の他社比較不可(企画書B2)・"両方使った人の体感"の純度を汚すため
  **不採用**(コア価値・ブランドに反する)。
- **#2(設計通り/一部誤解) ソロ大量入力が報われない**: 本人回答は相対マップ(catalog/switch)に反映され「あなたの判定」(/compare)・
  /api/my-feel で本人に見える。ゲートされるのは**コミュニティ集計(/battles・診断の該当率)のみ**で、これは水増し防止の設計。
  サイクル21の被覆閾値も、よく比較したラバー(実比較3件以上)なら数値が出る。「全部ロック」は被覆が薄い時の見え方。
- **#4(対応済み/パラメータ誤り) 分布が1軸固定・sort無視**: 分布の軸切替はサイクル20で `?axis=` をサーバ初期軸として honor 済み。
  評価者は `sort=hardness` を使用(分布は `?axis=` が正)。2軸プロット(硬さ×回転)は未実装の機能要望→M14。
- **#5(機能要望=M13) 張替履歴がupsertで上書き**: (sessionId,equipmentId,side)がユニークのため、同一ラバーを期間を変えて2エントリで
  残せない。頻繁に張り替える沼の遍歴管理ニーズ。加えて既存現用ラバーを isCurrent:false で再POSTすると現用が静かに0本化する
  端ケース(明示的なisCurrent:false指定時のみ)。履歴モデルの見直しとして整理。
- **#3(機能要望=M12) 軸の粒度**: 球持ち・引っ掛かり・弾みの質など近縁を分ける軸が無い(現状6軸に集約)。DBに winnerBallHold 列は
  あるが出題/表示から外している。沼セグメント向けの解像度強化。
- **#6(ほぼ対応済み) 完了感がない**: ギア内出し尽くし後、play-client は「ギア内の比較は出し尽くしました」バナーを表示(サイクル18)。
  評価者は生API連打で無限の explore を観測したが、UIには完了シグナルあり。
- **結論**: 最も警戒した#1は**エンジンが健全であることを検証で確認**(符号正常・体感駆動は設計通り)。新規の実装バグはなし。
  残るは沼セグメント向けの機能要望(M12軸粒度/M13張替履歴/M14二軸プロット)で、いずれも製品判断を要する。必需4の主因は
  依然コールドスタート(実データ量)。

### サイクル22 (独立評価16回目。新ペルソナ=日本式ペン・片面。グリップ前提バイアスの検証=実装バグなし)
独立評価(田中健一・38・中級・日本式ペンホルダー・片面(フォアのみ)・前陣速攻・表ソフト・**必需6/手軽8/手間7**)。
「シェーク・両面貼り前提の構造バイアス」を疑って投入したが、**片面ペン対応は堅牢で構造バイアスは検出されず**(表/裏の
種類混在のような欠陥は無し)。デザイン刷新後も片面動線は壊れていないことを確認した検証サイクル。
- **確認できた良い点**: ホーム/カタログ/詳細/マップ/switch に「シェーク」「フォア+バック2枚前提」の文言ゼロ(grep 0件)。
  FHの表ソフト1〜2本だけの登録で全機能成立(「バック面を登録して」等の無関係要求なし)。A/B出題34問すべて表ソフト同士。
  /switch がFH現用基準で後継表ソフトのみ候補化。診断も14問で「前陣速攻型」と正しく判定。
- **指摘①(最重要・コード変更不可=GTM) コールドスタート**: 仕組みは片面ペン・表ソフトに正しく機能するが、表ソフト同士の
  「両方使った」回答件数が無く、候補の体感差が「集計中/あと2件で公開」だらけ。実データ量の問題で偽データは禁じ手。
- **指摘②(コード変更不要=評価方法のアーティファクト) 「答えるにはオンボーディング必須(401)で行き止まり」**: 評価者は生API
  `GET /api/question` を直接叩いて401(onboarding_required)を観測したが、ブラウザ動線の `/play` は未登録だと **307→/onboarding**
  にクリーンにリダイレクトする(実走確認済み)。ナビ「答える」→オンボーディングへ自然に着地し、行き止まりは存在しない。
- **指摘③(将来課題=M11に記録) グリップ/片面という属性を保存できない**: onboardingは level/playstyle/bladeCategory のみで
  グリップ(ペン/シェーク)も「片面のみ」も項目なし。**今は実害なし**(FH登録だけで成立)。ただしコミュニティが育って
  「同じ片面ペンの人の比較に絞る」をやる段階では、この軸が無いと片面ペン特有の体感を抽出できない。
- **結論**: 必需6はコールドスタート(実データ量)が主因で、片面ペンに対する**実装は正しく機能している**(種類整合・FH単独成立・
  後継候補導線すべてOK)。新規の実装バグはなし。残る壁は実ユーザーのデータ密度(GTM)に収束。

### サイクル21 (独立評価15回目。新ペルソナ=初心者。デザイン刷新後/カタログのn<3飽和表示バグを発見)
独立評価(鈴木大樹・15・卓球部3ヶ月・初級・戦型未定・**必需3/手軽7/手間6**)。
デザイン刷新(プリミティブ/ミニマル化)後の初評価。手軽さ7=「ホームで用途が3秒で伝わる」「登録なしで触れる」
「A/Bのpromptが"イメージでOK:弧線が高そう(山なり)なのは?"と専門語に噛み砕き」を高評価。正直さ(集計中・データ少なめ・
断定しない)も信頼と評価。実装で直せる指摘1件を対応、残りはコールドスタート/要・製品判断として記録:
- **指摘①⑤(最重要・実装で直した) カタログ/分布のスコアが n=1 で「100/0」に飽和し『満点ラバー』と誤読**:
  他画面では徹底している n<3 非断定ルールが、カタログのスコア帯(`ScoreStrip`)と分布(`MapBars`)に未適用だった。
  正則化アンカーで全ラバーにスコアが付くため、実比較0-2件でも 0-100 の数字(多くが100/0に飽和)が出て、
  比較サービスの中核が初心者には「壊れている/嘘」に見えていた。
  - 修正(`catalog-explorer.tsx`): 実比較<3件は数値を断定せず「少」表示・バーをグレー縮約。一覧/分布とも凡例に
    「『少』は実比較3件未満でまだ位置を確定できない」を明記。実走(playwright)で n=1 が「少」表示になることを確認。
- **指摘②(要・製品判断=M8に記録) 基準ゼロ・用具不明の初心者に"次の1枚"が出ない**: 10問答えても診断の出口が空の
  カタログに戻るだけ。評価者の推奨=公称スペック+初級プロフィールで「はじめの1枚(参考)」を実データ0でも提示。
  ただし「初心者は◯◯すべき」(ブランドNG)に触れうるため製品/企画判断が要る → planning-anchor で方向確認の上で着手。
- **指摘③(構造・M9に記録) 自分の用具が分からない初心者は基準を作れず詰む**: 型番不明・貸出用具のため検索で特定できない。
  「持っていない/分からない」向けの基準の置き方(価格帯・初心者キット起点)が無い。②と同根のコールドスタート橋渡し課題。
- **指摘④(UX改善・M10に記録) 専門用語の壁**: A/Bのpromptには噛み砕きがあるが、カタログ/詳細は「弧線」等が裸。用語集/
  ツールチップや「結局どんな性格か」の一言要約が無い。`RUBBER_AXES`の low/high 記述(直線的/山なり等)を露出すれば軽量に改善可。
- **結論**: 必需3の主因はコールドスタート(初心者には実データ密度が特に効く)。実装欠陥だったカタログのn<3飽和表示を是正し、
  全画面で非断定ルールを一貫させた。残る②③④は実装で橋渡しできるが、②は企画判断を要するため次段で planning-anchor 起点に。

### サイクル20 (独立評価14回目。新ペルソナ=守備型・粒高使い。診断の攻撃志向断言バグを発見)
独立評価(山田徹・41・上級・カット主戦型・フォア裏ソフト/バック粒高・**必需5/手軽7/手間6**)。
少数派の守備型ペルソナでも**種類分離(サイクル18-19)は合格**: /catalog?category=RUBBER_PIMPLE_LONGは粒高23件ぴったり(混入ゼロ)、
A/B出題は全て粒高同士、/switch?base=粒高 は粒高だけ提案、用具詳細は「同じ種類の中での位置・データ少なめ」を正直表示、
守備のしやすさ(defenseEase)軸も粒高ペアで出題されると確認。サイクル19のカタログ種類分離も「分布も種類隔離が正しい」と評価。
実装で直せる指摘1件を対応、3件は事実確認の結果コード変更不要と判断:
- **指摘①(最重要・実装で直した) スタイル診断がカット型を「攻撃志向型」と断言**: playstyle:CUTで登録し粒高2枚を使う
  守備プレイヤーに「バランス・攻撃志向型」と表示。一方で傾向スコアは「まだはっきりした傾向は出ていません」と保留=矛盾。
  原因(`diagnosis.ts`): `controlLabel = controlVsPower > 0.3 ? "コントロール志向" : "攻撃志向"` で、control信号が
  無い(≒0)と無条件に「攻撃志向」へ倒れていた。軸指定回答中心のカット型はcontrol≈0のため必ず攻撃志向と誤表示。
  - 修正: ラベルを強い傾向が出た軸だけで構成する三分岐に(control>0.3→コントロール志向 / <-0.3→攻撃志向 / 中間は出さない)。
    どの軸も弱いときは断言せず、申告プレイスタイルを尊重した中立ラベルにフォールバック(CUT→「守備（カット）型」/
    DRIVE→「ドライブ型」/QUICK_ATTACK→「前陣速攻型」/既定→「バランス型」)。`diagnose(answers, playstyle)`に拡張し
    /diagnosis・/me が申告スタイルを渡す。回帰防止テスト2件追加(弱信号で攻撃志向と言わない/カット型は守備型に)。
  - 検証(実走): CUT登録→14問回答(弱信号)で /diagnosis が「守備（カット）型」を表示・「攻撃志向」消滅を確認。
- **指摘②(コード変更不要=既出。設計通り) 裏×粘着の出題に粘着ラベルが無い**: /playの選択肢カードは
  `EQUIPMENT_CATEGORY_LABELS`で各選択肢に「粘着ラバー」「裏ソフトラバー」を既に表示済み(評価者はAPI JSONのみ確認)。
  裏+粘着を同グループ扱いするのはサイクル18の確定方針(テンション⇔粘着の乗り換えが一般的)。UI変更不要。
- **指摘③(軽微改善で対応) 分布の軸切替がURL/SSRに乗らない**: 軸切替はクライアントJSで動くが、`?axis=`での
  URL共有・SSRでは既定(speed)軸しか出せなかった → `?axis=`を初期軸として honor(`catalog/page.tsx`・
  `catalog-explorer.tsx`)。守備軸の分布をURLで共有可能に。※評価者も「ブラウザでは動く可能性・純粋なバグではない」と注記。
- **指摘④(コード変更不要=設計判断/将来課題) /switch初期基準がフォア裏ソフト固定**: カット型はバック粒高を乗り換えたい。
  ただし「基準を切り替え」チップで粒高にワンタップ変更でき(`?base=`)、評価者も実機確認済み。多数派はフォア基準が
  自然なため既定変更は影響大。プレイスタイル/BHギアに応じた初期基準の最適化は将来課題としてバックログに記録(下記M6)。
- **指摘⑤(コード変更不可=GTM) 後継粒高を確定できない**: 粒高同士の「両方使った人」の実比較がほぼゼロ=コールドスタート。
  評価者自身が「UX・ロジックは十分良い、必需9に届かない唯一の理由は実データ量」と明言。偽データは禁じ手。
- **結論**: 必需5の主因はコールドスタート(少数派の粒高は特にデータが薄い)。実装欠陥だった診断の攻撃志向断言を潰し、
  カット型を含む全戦型に誠実な診断になった。残る5→9の壁は実ユーザーのデータ密度(GTM)に収束。

### サイクル19 (独立評価13回目。表ソフト使い再評価。サイクル18の種類分離をスコア計上)
独立評価(佐藤美咲・28・中級・前陣速攻・フォア表ソフト/バック裏ソフト・**必需5/手軽8/手間6**)。
サイクル18で「自己評価は計上しない」として保留していた種類分離のスコアを、同ペルソナの独立評価で計上。
**サイクル18の種類分離は独立評価で確認・合格**: /api/question を18回引いても表×表/裏×裏のみで種類混在出題ゼロ、
用具詳細は「同じ種類のラバーの中での位置・2本中1位」と種類隔離、分布(/catalog?view=map)も種類隔離が正しい、
と評価者が実機確認。手軽さ8(全API 200・動線が上級者でも納得)。
評価で出た**実装で直せる指摘1件**を対応し、**2件は事実確認の結果コード変更不要(評価方法のアーティファクト/誤診)**と判断:
- **指摘①(最重要・実装で直した) カタログ「一覧/分布」ビューだけ種類分離が繋がっていなかった**: 詳細・出題・/switch・
  順位は種類隔離済みなのに、`/catalog`(旧/map統合先)の一覧・分布だけは基準=表ソフトでも全271本(裏/粘着/粒高/アンチ)を
  混ぜて「近い順」に並べ、`?category=`指定も無視していた。種類隔離ロジックは存在するのに一覧ビューだけ未接続=典型的な取りこぼし。
  - 修正(`catalog-explorer.tsx`/`catalog/page.tsx`): 基準ラバーがあるとき、一覧・分布のラバーを基準と同じ比較可能
    グループ(表/裏系/粒高/アンチ)に限定(`rubberGroup`)。基準バナーを「{種類}の中で『{基準}』基準」に明示。
    `?category=`を初期種類フィルタとして honor(明示`?base=`＞`?category=`＞現用の自動基準の優先順位で衝突回避)。
  - 検証(実走/build): 表ソフト現用で一覧・分布が表ソフトのみに収束。lint/43テスト/build green。
- **指摘②(コード変更不要=評価者の誤診) 身に覚えのない幽霊ギア(テナジー05 BH)が混ざる**: DBを直接照合した結果、
  評価者自身が 05:57:46 のバーストで「BHテナジー05」を登録していた(gear POSTは要求された1本しか作成しない=
  自動追加経路は存在しない)。評価者の「BHはロゼナだけ登録した」は自己記憶違い。コード健全。
- **指摘③(コード変更不要=評価方法のアーティファクト) 診断の10問ゲートに到達できない**: HTTPで実フロー
  (GET /api/question→POST /api/answers)を再現すると answerCount は 6(表×表の全6軸)→explore へと着実に伸び、
  10問ゲートに普通に到達(explore も全て表ソフト同士で種類混在なし)。評価者の`count=3`頭打ちは、同一ペア×軸を
  再POST(revisionは加算されない)し新しい質問を取得しなかったため。診断ページも閾値未満時に「比較に答える→/play」で案内済み。
- **指摘④(コード変更不可=GTM) 乗り換え候補が空**: コールドスタート(実データ密度)。サイクル14/16/17と同じ構造的要因で偽データは禁じ手。
- **指摘⑤(実害なし) 生日本語URLで/api/equipmentが400**: ブラウザは自動エンコードするためUI利用に影響なし。記録のみ。
- **結論**: 必需5の主因はコールドスタート(候補が空)。実装欠陥として残っていた一覧/分布の種類混在を潰し、種類分離を
  全画面で一貫させた。診断ゲート・幽霊ギアは事実確認で実体なし(規律通り誤診はコード変更しない)。残る5→9の壁は
  実ユーザーのデータ密度(GTM)に収束。
- 次の論点(必需5→9): (a)コールドスタートでも候補に出すための公称スペック由来の暫定的近傍提案(「データが集まるまでの参考」明示)、
  (b)基準未設定時の分布ビューの種類混在(現状は基準ありのみ隔離)、(c)実ユーザー獲得のGTM施策。

### サイクル17 (独立評価11回目。クリーンDB・コールドスタート。WITB/基準モード実装後)
独立評価(高橋健太・必需3/手軽7/手間6)。手軽さは「動線・表示・検索が上級者でも納得」と高評価
(検索の表記ゆれ耐性・経験フラグ自動判定・現用フォアの乗り換え基準自動同期・基準切替を確認)。
必需が3に下がったのはクリーンDBで「他人の比較が0件」のコールドスタートを引いたため(=構造的)。
評価で出た**実装で直せる指摘3件 + 評価前に発見した致命バグ1件**を実装で潰した。
- **(評価前に発見) ビルド破損**: 直近コミットで GearItem.bladeWeightGrams を足したが /gear 行編集の
  onSave 型に含めず、next build が型エラーで失敗する状態でpushされていた → patch 型に追加して解消。
- **指摘① 対決ランキングの看板倒れ**: /battles が常に「まだ対決データがありません」で機能が死んで
  見えた → 載る条件(両方使った人3件)を明示。空状態を「集計中N件・両方使った2本で公開が近づく/
  あなたが最初の対決データに」と二分岐し、なぜ即反映されないか(公平のため両方使った人基準)を明記。
- **指摘② マイページの匿名行き止まり**: ヘッダー『マイページ』は匿名でも出るのに /me が userId 無しで
  /login へ即リダイレクトし、ギア登録済みでも壁に当たった → /me を sessionId でも描画。匿名でも
  プロフィール・最近の回答・体感メモ・ギア導線を表示し、登録は『端末間引き継ぎの任意アップグレード』に。
- **指摘③ 似た用具がコールドスタートで破綻**: 疎データで正則化アンカーが全スコアを中央に潰し、
  上位機(ディグニクス05)に入門機(ロゼナ)を「似てる」と推して信頼毀損 → findSimilarEquipment の
  距離計算を「基準・候補双方がその軸で実比較2本以上」の軸に限定。根拠が薄ければ出さない(正直に空)。
  確信度 support を付与し詳細ページで「データ少なめ」を明示・断言コピーを緩和。
- **指摘④ 乗り換えが本来の目的(05の後継探し)に応えない**: 同格帯候補への差分が出ない。これは
  サイクル14/16と同じ**実データ密度(コールドスタート)**で、偽データ禁止のためコードでは直せない=GTM課題。
- **結論**: 手軽さ7・手間6で器は引き続き堅い。必需3はコールドスタートの構造的現実。実装で直せる
  信頼毀損(看板倒れ・偽の似た用具・匿名行き止まり)は潰した。残る壁は実ユーザー獲得(GTM)に収束。

### サイクル18 (独立評価12回目。新ペルソナ=表ソフト使い。ラバー種類の混在バグを発見)
独立評価(佐藤美咲・28・中級・前陣速攻・シェーク・フォア表ソフト/バック裏ソフト・必需4/手軽7/手間6)。
上級裏ソフト視点では出なかった**系統的な本質バグ**を新ペルソナが発見=アプリが裏ソフト前提で、
**ラバー種類(表/裏/粒高/粘着/アンチ)を分けずに**比較・順位・候補・出題を混ぜていた。
表ソフトは構造上スピン・弧線が出ず、裏ソフトと横並び比較しても無意味で信頼を毀損する。
評価者の再現:
- /switch(表ソフト基準)の後継候補が裏ソフト(マークVHPS/DNAプロM)・粘着(ラクザZ)ばかり=戦型が変わる。
- /api/question が 表ソフト×裏ソフト を「スピン/弧線どっちが上?」と出題(比較不成立)。
- /equipment の相対順位が「表ソフトが弧線で全ラバー中2位」=表/裏を混ぜたプールで順位付け。
- 9点への決定的指摘=「ラバーの種類でプールを分けていないこと。同種内に限定すれば一気に必需級」。
**対応(実装で直せる=即対応。コア機能の信頼性に直結)**: 『比較可能グループ』を導入し相対比較・
候補・順位・出題を同グループ内に限定。裏ソフトと粘着は同じ裏面構造でテンション⇔粘着の乗り換えが
一般的なため同グループ。表/粒高/アンチは独立。`rubberGroup`/`sameRubberGroup`/`rubberGroupCategories`
(types)、`axesForPair`が種類違いに空を返す(axes)、explore出題を同グループ限定(question-generator)、
ratingsをグループごとに独立正規化+各consumerに同グループガード(relative-map)、/switchワンタップ候補も
同グループ限定。用具詳細コピーを「同じ種類のラバー中」に。
**検証(実走)**: 表ソフト現用で/switch・ページ全体に裏ソフト参照0、出題20問すべて表ソフト同士。
裏ソフト現用は20問すべて裏/粘着(回帰なし)。グループ不変条件テスト2件追加。lint/43テスト/build green。
**結論**: 必需4は混在バグ(=実装欠陥、コールドスタートではない)が主因で、それを潰した。
種類分離はコア価値(相対理解)の前提であり、次の独立評価でスコア反映を計上する(自己評価は計上しない運用)。

### サイクル13 対応 (独立評価サイクル12の指摘3件。実装済み・実走で検証)
独立評価が必需3→7に改善(サイクル12「あなたの判定」が効いた)。新たな最大課題3件:
- **top1(最優先) 「出し尽くしました」の誤表示**: FH+BH現用2本は同面ペアを作れず
  実体験対決が0件なのに、/play が「ギア内の比較は出し尽くしました」と表示し、
  来訪者の第一印象(=両方使った人の比較への期待)を裏切っていた。
  - 修正: `play.ts` に gearPairCount(同面2本以上のペア数)を追加し、
    `play-client.tsx` の文言を分岐。ペアが作れていない時は「これはイメージ回答です/
    フォアとバックの2本は両方使った比較にならない/**同じ面にもう1本**使ったラバーを
    登録すると実体験比較(最大5問)が出せます」と正しく案内。
- **top2 /switchが回答済み候補を反映しない**: ロゼナ×ラクザ7を実体験回答済みでも
  乗り換え画面が空状態のまま(候補は手動?c=指定のみ)。本命画面が入力を活かせず。
  - 修正: `switch/page.tsx` で候補未指定時、自分が基準ラバーと比較回答済みの
    ラバーを自動で候補表示(直近順・最大3)。「あなたが『X』と比較した候補」と明示。
- **top3 回答API tally の軸バグ**: `tallyPair` が winnerOverall 固定で、spin/球持ちを
  答えても回答直後のフィードバックが好みの集計を返していた。
  - 修正: `data.ts` tallyPair に axis 引数を追加し答えた軸の列を集計。
    answers route が question の axis を渡す。switch候補集計は overall のまま。
- 検証(実走): FH+BH2本で /play が新文言を表示 / spin=B回答で tally が b 側計上 /
  /switch?base=ロゼナ がラクザ7を自動候補表示することを確認。lint/test/build green。
- 残課題(次の独立評価向け): 必需9へはコミュニティの実データ密度が要 /
  匿名セッション量産の完全防御(ユニークユーザー集計+レート制限+メール認証)。

### サイクル12 対応 (独立評価サイクル11の最優先: 自分の回答が閾値で隠れる行き止まりを解消)
- 独立評価(必需3/手軽6/手間5)の最優先指摘: 「両方使った人の比較」を求めて
  自分でロゼナ vs ラクザ7 を全軸回答しても、各軸が公開閾値(n≥3)未満で
  「集計中・あと2件で結果公開」のまま隠れ、**自分の判定すら見られない**。
  サービスの存在理由を直撃する致命傷。
- 修正 (`src/app/compare/[aId]/vs/[bId]/page.tsx`):
  - **あなたの判定カード**を追加。公開閾値・絞り込みに関係なく、本人の軸別判定
    (好み/速さ/回転/硬さ/球持ち)を勝者ラバー名で常時表示。両方使用なら
    「両方使用」バッジ。「これはあなた自身の記録です。下の集計は他の人の回答が
    集まると公開されます」と、本人記録と公開集計の役割を明示分離。
  - 公開集計の母数を「行数」→「実際に答えた人数(distinct session)」に是正。
    1人が軸別に答えると行が増え n が水増しに見える問題(評価者が指摘した
    「n=5だが実は1人」)を解消。バナーを「参考程度に(回答者X人)」に。
  - 公開側の n≥3 閾値・%非断言の誠実さは維持 (本人記録だけを別枠で返す)。
- 検証 (実走): ロゼナ vs ラクザ7 を5軸回答後に compare を取得し、
  あなたの判定5軸(全てロゼナ)+両方使用バッジが表示され、公開集計は
  「回答者2人・集計中」のまま誠実に保たれることを確認。
- 残課題(次の独立評価向け): /switch初期基準が現用FH固定でBHに手動切替が必要 /
  マイギア登録した候補が/switch候補に未反映(導線分断)。

### サイクル11 対応 (B3本丸: バック面1本の出題行き止まりを解消。実装済み・実走で検証)
- 原因: explore 出題の基準(base)が「現用FHラバー1本」に固定されており、
  フォア現用+バック1本のユーザーは、同面ペアも作れず(同面2本以上が必要)、
  バック面のラバー(=乗り換え本命のことが多い)が **一度も出題されない** 構造だった。
- 修正 (`src/lib/question-generator.ts`): explore の基準を所有ラバー全体で巡回。
  ペア登場回数(=実体験データの厚み)が薄いラバーを優先して基準にする。
  これで単独面のラバーも「(本命) vs 人気ラバー」のイメージ比較が出題され、
  /switch?base=(本命) の乗り換え検討に必要なデータが育ち始める。
- 検証 (実走): 上級・FHディグニクス05(現用)+BHロゼナ(1本)で /api/question を
  回答付きで20回ループ。修正前は全問ディグニクス基準だったが、修正後は
  ディグニクス基準とロゼナ基準が交互に出題されることを確認。
  /switch?base=ロゼナ もバック面コンテキスト付きで候補表示まで到達。
- テスト追加: 「フォア現用+バック1本でもバック面ラバーが explore 基準として
  出題される」を question-generator.test.ts に追加 (18 tests green)。
- 残課題(次サイクル以降・サイクル10から継続): 匿名セッション量産の完全防御
  (ユニークユーザー数集計+レート制限+メール認証) / questionId未検証。
  次の独立評価でスコアを計上する(実装者自己評価は計上しない運用ルール)。

### サイクル10 対応 (看板・ランキングの水増し耐性。実装済み・攻撃再現で検証)
- 公開集計を詐称不能な実体験(BOTH)基準に統一: aggregatePairs に
  experiencedOnly を追加し、/battles ランキング・トップ「注目の対決」・
  用具ページの対決一覧・用具の「好み勝率」をすべて BOTH のみ集計に。
  トップ看板を「累計AB比較回答(全票)」→「両方使った人の判定(BOTH数)」に変更
- 検証: 評価者と同じ攻撃(使い捨て匿名セッション8個から投票)を再現し、
  公開面のn・ランキング順位が一切動かないことを確認(ボット票はNEITHERで除外)
- 残課題(次サイクル以降): ①各使い捨てセッションで偽ギアを登録すればBOTH化でき
  完全防御には至らない→ユニークセッション/ユーザー数での集計+レート制限+メール認証が必要
  ②questionId未検証(出題外ペアに投票可、経験フラグで実害は限定的)
  ③B3本丸=バック面1本のユーザーに質問が生成されない構造的行き止まり(最優先)

### サイクル9 対応 (①②③⑤実装済み・検証済み)
- ① 票の水増し防止: 同一セッション×ペア×軸の再回答は「最新で上書き」
  (A/B逆順の意見変更も勝者を反転して正しく上書き)。連投してもn不変を確認
- ② アカウント引き継ぎ: User.primarySessionId を追加。signup時に紐付け、
  login時にcookieを本体セッションへ付け替え+匿名データをマージ。
  旧アカウントは userId 紐付きデータからセッションを復元
  (健太: 別デバイスでギア4本・回答28件・上級プロフィール復元を確認)
- ③ パスワード認証: scrypt (Node組み込み) で User.passwordHash。
  signup必須(8文字以上)・login照合。旧アカウントは初回ログインで設定(救済)。
  gear作成時に userId も保存 (復元の手がかり強化)
- ⑤ LPコピー修正: 「両方使った人だけの比較」→「『両方使った人』に絞れる比較」
- ④ 厚さ・面別の集計絞り込み (M2) は未実装 — 次サイクル以降の最優先
- 構造的限界(コードで解決不可): 必需9にはコミュニティの実データ密度が必要。
  「ロゼナ後継に答えられない」のはデータ不足であり、偽データ投入は禁じ手

### 評価の運用ルール (今回の学び)
- スコアは実装者の自己評価では計上しない。会話履歴・修正内容・合格基準を
  渡さない独立評価者がHTTP実走のみで採点したものだけを記録する

### サイクル8 結果 (最終評価・実装変更なし)
- 公開遷移のE2E確認: 第三者2セッションで「好み」がn=4に到達し、
  /switch候補カード(75%/25%)・検討まとめ(25%がこちら派)・用具ページ・
  ランディング・/battlesランキング1位に一斉反映されることを確認
- /compare は「あなたと似た人」絞り込みが既定のため同データでもn=2と
  表示 — 絞り込みの正直さとして仕様通り (解除導線あり)
- 残課題は運用系のみ: M1(シェアOGP) / M2(同条件絞り込み) /
  M4(battles・compare/select整理) / O1(本番スモーク) / O2(EC API)

### サイクル7 対応 (全て実装済み)
- 再出題バグ修正: 既出(ペア×軸)の除外が「直近20件」のみだったため、
  回答が20件を超えると古い質問が再出題されていた。回答済みの組み合わせを
  恒久除外に変更 (※コミットメッセージの「1行に複数軸」という原因記述は
  誤りで、実際は1回答=1行・原因はキャップ。修正自体は正しい)。
  出し尽くし後は explore のみが出題されることを確認
- /switch に「検討まとめ」テーブル (候補2つ以上で表示):
  みんなの好み(n>=3のみ%)・公称硬度差(同メーカーのみ)・価格差を1表に
- /me に「あなたの体感メモ (Xペア分)」常設導線 (B1完結)

### サイクル6 対応 (全て実装済み)
- 用具ページ→検討の直結: ラバーの用具ページに「乗り換え検討の候補に
  入れる」(未所持) /「これを基準に乗り換えを検討する」(マイギア) CTA
- M3: ギア登録フォームに「よく登録されているラバー」ワンタップチップ
  (登録実績順 + 定番ラバーで補完。/api/equipment?popular=1 を追加)
- /switch ワンタップ候補のフォールバック: 対決データのない基準でも
  「よく使われている:」チップで検索ゼロから検討を始められる

### サイクル5 対応 (全て実装済み)
- B3本丸: /switch の基準をマイギアのラバー間でワンタップ切り替え
  (?base= パラメータ。FH/BHの面ラベル付きチップ、候補は引き継ぎ)。
  基準カードに「フォア面・いま使用中」等の面文脈を明示
- n断言の最終一掃: /switch 候補カードの「みんなの好み」と
  /compare の軸別バー・インサイト文を VersusBarOrPending 系の
  n>=3 ルールに統一 (n=1の「100%」断言を全廃)
- B1仕上げ: /me「最近の回答」に軸ごとの自分の判定チップ
  (好み/速さ/回転/硬さ/球持ち) と実体験バッジを表示

### サイクル4 対応 (全て実装済み)
- プロフィール一貫性: /api/onboarding がログイン済みなら User も同期。
  /me は SessionProgress を優先表示 (既存アカウントのデフォルト値残留を救済)。
  /me に「プロフィールを編集」導線 + /onboarding 再訪時に回答プリフィル (GET追加)
- 診断のコールドスタートの正直さ (B4): 「全プレイヤーのX%が該当」は
  診断者10人以上のみ表示 (未満は「診断者10人以上で公開」)。
  傾向スコアの +0.0 行を非表示 (全行ゼロ時は「まだ傾向は出ていません」)
- BH面1本問題 (B3の一部): /gear で1本だけの面に
  「まだ比較質問を作れません。もう1本追加で+5問」のナッジを表示

### サイクル3 対応 (全て実装済み)
- /switch 基準未設定時にマイギアからワンタップ選択 (再入力撲滅)
- 候補追加を useTransition 化し「実体験データを集計しています…」を必ず表示
- 「好み」勝率は判定5件以上のみ% (未満は「収集中(判定X件)」)
- データを育てるループの可視化: 集計中表示・体感プロファイル・回答直後に
  「あとX件で公開」の進捗を統一表示

### サイクル2 対応 (全て実装済み)
- /switch: 候補追加は機能していることを再現確認 (516msで表示)。
  「無反応に見える」問題として loading スケルトンを追加
- n<3 の断言表示を全面禁止: 体感プロファイルは「まだ回答X件 — 集計中」、
  関連対決リスト(用具ページ/対決ビュー/ランディング)は集計中表示に統一
- ギア重複登録ガード: 登録済みラバー選択時に前回条件をプリフィル + 警告表示
- /gear のペア数見積もりを同面ペア基準に修正
- explore切替時にギア追加を強く誘導するバナー (実体験質問+5問の提示)

### サイクル1 対応 (全て実装済み)
- 厚さ上書きバグ修正 (再追加で既存詳細を保持)
- ギア出題を同面ペアのみに制限 + 直前ペア連続回避
- /battles・ランディングを n>=3 のみ表示 (未満は「集計中」)
- 独自0-100正規化値の表示を全廃 (公称硬度は「自社基準」明記、他社間は差分非表示)
- 勝率→「好み」勝率に明確化
- 回答の資産化: 開示パネルに+1表示、/gear に「あなたの体感メモ」(ペア×軸の自分の回答一覧、実体験バッジ付き)

### 評価基準 (ユーザー指示)
3指標すべて 9/10 以上で合格: ①絶対になくてはならない度 ②手軽さ ③手間のなさ

## PM仮説バックログ (ペルソナ評価と突き合わせて優先度決定)

### 大玉 (体験の根幹)

- [ ] **B1: 回答が資産にならない問題**
  10問答えても「自分に何が残るか」が見えない。マイギア間の自分の
  体感マップ (「自分はD05よりロゼナが好みだった」の一覧) を
  マイページ/ギアページに常設し、回答=自分のレビュー資産になる構造へ。
- [ ] **B2: 公称スペック数値の信頼性**
  0-100正規化した「公称値」は上級者には出所不明の数値に見える。
  メーカー間比較不能なのは業界常識。→ 数値表示をやめ相対表示にする
  / 「参考値」の出所を明示 / もしくは集計データのみに絞る判断。
  追記(M8): 初心者向け入口(はじめの基準)でも**公称数値の軸別順位は出さず**、
  定性カテゴリ(やわらかめ/コントロール寄り/手頃)+出所明示(「一般に言われる特徴・参考」)に限る。
- [x] **B3: バック面文脈の弱さ** (サイクル5でswitch基準切替、サイクル11で出題巡回を実装)
  裏裏ドライブでもF/Bでラバー選定基準は別物。switch は ?base= でFH/BHどちらの
  基準でも検討でき(サイクル5)、出題も所有ラバー全体を巡回してバック面単独の
  ラバーまでデータが育つ(サイクル11)。残: 体感翻訳の面別チューニングは将来課題。
- [ ] **B4: コールドスタートの正直さ**
  n=0〜1のバーだらけだと信頼を失う。データ無し時の表示哲学を統一
  (「まだ無い」を堂々と見せ、貢献を促す)。

### 中玉

- [ ] M1: 診断のシェア画像 (動的OGP) — 流入の核
- [ ] M2: 用具ページの「同条件レビュー」絞り込み (厚さ・面)
- [ ] M3: ギア登録の摩擦低減 (人気ラバーのワンタップ候補)
- [ ] M4: battles と compare/select の役割重複の整理 (改廃候補)
- [ ] M6: /switch 初期基準のプレイスタイル最適化 (サイクル20。カット型/バック変化系
      ユーザーはBH粒高・アンチが乗り換え本命のことが多い。現状はフォア現用固定で
      ワンタップ切替は可能だが、申告スタイルやBHギアに応じて初期基準を選ぶと第一印象が改善)
- [ ] M7: 基準未設定時の分布(/catalog?view=map)の種類混在 (サイクル19。基準ありは
      種類隔離済み。基準なしで複数種類にスコアが付くと1帯に混在しうる。種類選択を促すか帯を種類別に分ける)
- [x] M8: コールドスタートの初心者に「はじめの基準(参考)」を提示 (v1実装済み)
      planning-anchor の判断に従い、「推薦」でなく**相対地図に入るための原点候補(参考)**として実装。
      公称数値の順位は出さず、定評ベースの定番裏ソフトを2〜3枚(`lib/entry-anchors.ts`)。「べき」不使用・
      主語はユーザー・「参考」明示・実集計と視覚分離(`components/entry-point-anchors.tsx`)。主動作は
      「これを基準に用具マップを見る」(/catalog?base=)。基準ゼロ(currentRubberId無し)のとき /diagnosis の
      閾値未満画面と結果画面に表示。企画書 6.1/10.6/B2 に運用ルールを明文化。
      残(次段): /onboarding 直後・初回 /catalog・/me 空状態への展開、M9(用具不明の初心者)の受け口統合。
- [ ] M9: 用具が分からない/持っていない初心者向けの基準の置き方 (サイクル21。価格帯・初心者キット起点で
      基準ゼロでも検討を始められる導線。M8と同根のコールドスタート橋渡し)
- [ ] M10: 専門用語の噛み砕き (サイクル21。弧線/球持ち等の用語集・ツールチップ、用具詳細に
      「結局どんな性格か」の一言要約。RUBBER_AXESのlow/high記述を露出すれば軽量に改善可)
- [ ] M11: グリップ(ペン/シェーク)・片面/両面をプロフィールに保持 (サイクル22。今は実害なしだが、
      コミュニティが育った段階で「同じグリップ/片面の人の比較に絞る」を可能にする軸。導入時は
      既存の経験フラグ・種類分離と同じく集計の絞り込みオプションとして。必須項目にはしない)
- [ ] M12: 比較軸の解像度強化 (サイクル23。球持ち/引っ掛かり/弾みの質など近縁ラバーを分ける軸。
      DBに winnerBallHold 列はあり。出題が増える副作用と、上級者だけに見せる出し分けを検討。
      axes.ts(単一の真実)と question-generator/relative-map/詳細/履歴を貫通して追加すること)
- [ ] M13: マイギアの張替履歴モデル (サイクル23。同一ラバー×同面を期間違いで複数エントリ保持。
      現状は(session,equipment,side)ユニークで上書き。遍歴・重量・貼った日を時系列で残す。
      既存現用をisCurrent:falseで再POSTすると現用が0本化する端ケースも併せて是正)
- [ ] M14: 分布の二軸プロット (サイクル23。硬さ×回転など2軸の相対地図。現状は1軸バーのみ。
      ?axis= の単軸はサイクル20で対応済み。沼/上級者が一覧で性格を掴むための上位機能)

### 運用

- [ ] O1: 本番スモークテスト (ネットワーク許可後)
- [ ] O2: 楽天/Amazon API での実写真・実価格 (要ユーザーのAPIキー)
